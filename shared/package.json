{
  "name": "shared",
  "version": "1.0.0",
  "description": "Shared types and protobuf schemas for Birdy",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "proto:generate": "protoc --ts_out=src/generated --proto_path=proto proto/*.proto",
    "test": "jest"
  },
  "dependencies": {
    "protobufjs": "^7.2.5"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.0"
  }
}
